# _agent

Claude Code writes here and nowhere else in the vault. Reports from `/ideas`, `/trace`, `/emerge` and the rest land in this folder, dated. Read them, then copy whatever you agree with into your own notes in your own words.

`vault.py` ignores this folder, so the agent's writing never feeds back into the patterns it finds.
