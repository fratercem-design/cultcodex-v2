# cultcodex.me — Re-audit 2026-09-01

Follow-up to `AUDIT.md` (2026-08-28). Live HTTP + source on `master` @ `bbbbb632`.

## Delta since August 28

| Finding | Aug 28 | Sep 1 |
|---|---|---|
| F11 `/settings` 404 | Open | **Fixed** — `/settings` returns 200 |
| F4 observability | Open | **Partial** — `@sentry/nextjs` is in the tree; DSN still unset in this audit |
| F1 committed secrets | Open | **Still open** — Anthropic key + `ENRICH_SECRET` still in `scripts/enrich/browser-enrich-topics.js` |
| F2 `@auth/core` pin | Open | **Still open** — `overrides` still `"0.41.2"` |
| F2b duplicate `package.json` keys | Open | **Worse** — more duplicate keys; last-wins silently downgrades `@prisma/client` 7.10→7.9.1 and `@anthropic-ai/sdk` 0.122→0.120 |
| F5 CDN cache | Open | Mixed — legal/static pages now `PRERENDER`/`STALE`; archive templates still `force-dynamic` + `no-store` |
| Counters | 2,973 ep / 97% transcribed | 2,986 ep / **96%** transcribed (ingest outrunning ASR) |

## This PR ships

1. Strip committed credentials from the browser enrich script (rotate the live values **before** merge).
2. Deduplicate `package.json` dependencies; keep the newer of each pair.
3. Move `@auth/core` override to `^0.41.3`.
4. Coerce `limit` / `threshold` / dates inside `semanticSearch`; type the raw-SQL rows.
5. Process-local LRU cache on `embedOne` (256 entries).
6. `User-Agent: *` now disallows member/account surfaces (`/members/`, `/red-room/`, `/settings/`, `/user/`, `/claim/`, `/salon/`, `/onboarding/`).
7. Footer column labels are `<p>` instead of `<h2>` so they no longer precede the page `<h1>`.

## Still operator-only (do not skip)

1. **Rotate** the Anthropic API key and `ENRICH_SECRET` in Vercel. Treat both as compromised since 2026-04-29.
2. Set `NEXT_PUBLIC_SENTRY_DSN` (and `SENTRY_AUTH_TOKEN` if you want source maps).
3. Confirm Xata PITR / add a scheduled `pg_dump` to R2.
4. Baseline the 20 models that have no Prisma migration.
5. Drop `force-dynamic` on episode/topic/lore/quest templates so ISR can run.

## Live snapshot (2026-09-02 00:28 UTC)

- Home 203 KB, TTFB 142 ms, `no-store`
- `/episodes/get-your-freak-on-friday` **2.86 MB**, TTFB 237 ms, `no-store`
- Sitemap 5.39 MB, **27,882** URLs
- Admin / cron / webhook gates still enforce 401/403
- Oracle trial still answers unauthenticated (by design; global daily cap is the real wallet guard)
- `www.cultcodex.me` → apex is **307** from Vercel edge, not the 301 in `middleware.ts` — fix in the Vercel domain settings
