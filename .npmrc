# Suppress peer-dependency hard failures from packages that haven't yet
# updated their peerDependencies range to include Next.js 16.
# npm ci on Railway runs without flags; this file applies project-wide.
legacy-peer-deps=true
